// Single-sketch example

function setup (){
  createCanvas (800, 600);
}

function draw(){
  background(100);
  fill(255);
  noStroke();
trianglemode(center)
  rectangle(mouseX, mouseY, 80, 20,20,20);
}
